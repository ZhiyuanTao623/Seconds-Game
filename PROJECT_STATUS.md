# SECONDS — 项目状态与协作交接

> 最后更新：2026-08-04
> 用途：这是 Codex 与 Claude Code 共同参考的**现状记录**。设计意图看 `DESIGN.md`；本文件只记录已经实现和已验证的内容。

## 项目概览

`SECONDS` 是一个 2D 灰盒动作速通原型。唯一货币是时间：游戏计时、受击惩罚、商店购买、捷径和修复都会计入最终成绩。

总成绩：`游戏时间 + 受击惩罚 + 消费 - 击杀返还`。

技术栈：TypeScript、Vite、Vitest；构建产物为单个自包含 HTML 文件（`dist/index.html`）。

## 已实现

- 8 层分支地图，首层战斗、倒数第二层休整、最终 Boss。
- 房间类型：普通战斗、精英、商店、时间修复站、捷径门、Boss。
- 战斗：移动、鼠标瞄准、近战、冲刺无敌、受击硬直、连击税、墙体碰撞。
- 敌人：冲锋兵、射手、重甲、医疗兵；以及拥有冲锋、环形弹幕、震波的两阶段 Boss。Boss 半血后进入「回收」阶段：冲锋反向折返、弹幕缺口轮转、震波由外向内回收；每个回收动作结束后短暂露出高伤害破绽。
- 13 个不可重复强化；包含数值型强化及掷刃、掠影、蓄力斩三种操作型强化。
- 精英房奖励为进化分支：从不同已拥有强化的未见分支中展示两张；展示过的分支本局不再出现。数值进化和解除代价分支可叠加，当前解除代价适用于利刀、韧体、连斩。
- 全局 seed：地图、房间、敌人出生点、强化抽取和 Boss 招式均可复现。
- 中英文 UI 与本地语言记忆。
- 暂停为全遮挡式暂停；地图、奖励和商店正常计时。
- 走表规则（2026-08-04）：玩家能操作或作决定时时间才计入成绩。房间清空后的 0.45s
  过场（纯动画）停表；顿帧期间账本按世界速度（×0.12）打折。受击僵直不停表（惩罚
  本身），「时停」不打折（玩家照常操作）。场景通过 `Scene.timeScale` 报价，App 主
  循环按它缩放走表；`tests/timeAccounting.test.ts` 钉住这两条规则和两个反例。
- 精英房数值已降过一次难度（2026-08-04）：重甲数量公式从 `2+floor(层数/2)` 放缓到
  `1+floor(层数/3)`，血量倍率从 ×1.35 降到 ×1.2。floor 6 精英房从「5 重甲+8 杂兵+1
  医疗兵=14 只、重甲单个 105.3hp」降到「3 重甲+8 杂兵+1 医疗兵=12 只、重甲单个
  93.6hp」。杂兵/医疗兵数量和数值都没动。加了 `tests/eliteBalance.test.ts` 钉住
  重甲数量公式和总数上限，以后再往精英房加东西记得先看这个文件。

## 已验证

2026-08-04 已执行并通过：

```text
npm test       # 12 个测试文件，106 项测试全部通过
npm run build  # TypeScript 检查和 Vite 构建通过
bash tests/run.sh  # 推送闸门（类型检查 + 测试 + 生产构建）全部通过
```

现有自动化覆盖：定价一致性、随机性/seed 可复现、游戏时间轴、地图约束、医疗兵行为、精英房数量护栏、Boss 回收阶段、精英进化、战斗循环、走表规则（过场停表/顿帧打折）、i18n、启动冒烟测试。

## 发布约定

- 每次完成并验证代码或文档改动后，必须提交并推送到 GitHub 的 `main` 分支；不要只保留本地改动。
- 每次改动完，都要在这份文件里更新对应内容，让 Codex 和 Claude Code 看到的现状是一致的。
- `tests/` 已经**整个进仓库**（2026-08-04 起，之前只有部分文件被单独 force-add，导致 CI 和本地闸门跑的不是同一套测试）。以后新增测试文件正常 `git add` 即可，不用再手动绕过 `.gitignore`；CI（`.github/workflows/deploy.yml`）现在跑的就是 `bash tests/run.sh` 本地闸门那一整套。

## 关键文件索引

| 目的 | 文件 |
| --- | --- |
| 目标玩法规格 | `DESIGN.md` |
| 当前协作状态 | `PROJECT_STATUS.md` |
| 全部数值配置 | `src/game/config.ts` |
| 强化池 | `src/game/upgrades.ts` |
| 时间账本与总成绩 | `src/game/ledger.ts` |
| 地图生成和路线约束 | `src/game/map.ts` |
| 房间敌人编排 | `src/game/room.ts` |
| 战斗世界与受击/伤害 | `src/game/world.ts` |
| 各场景流转 | `src/scenes/` |
| 测试 | `tests/` |
