# SECONDS — 项目状态与协作交接

> 最后更新：2026-08-04
> 用途：这是 Codex 与 Claude Code 共同参考的**现状记录**。设计意图看 `DESIGN.md`；本文件只记录已经实现和已验证的内容。

## 项目概览

`SECONDS` 是一个 2D 灰盒动作速通原型。唯一货币是时间：游戏计时、受击惩罚、商店购买、捷径和修复都会计入最终成绩。

总成绩：`游戏时间 + 受击惩罚 + 消费 - 击杀返还`。

技术栈：TypeScript、Vite、Vitest；构建产物为单个自包含 HTML 文件（`dist/index.html`）。

## 模组与进化系统 v3（进行中，里程碑 4/5 已完成）

`DESIGN.md` 已从「13 个平铺强化」的 v2 重写为「开局三选一模组 + 每个强化只有基础/
进化两阶段」的 v3。实施按 5 个里程碑推进，见对应的实施计划。**当前完成到里程碑 4**
（框架 + 通用强化池 + 飞刃/掠影/蓄势三个模组全部落地）；只剩里程碑 5：地图新约束、
Boss 输出窗口、HUD/结算改版。

**里程碑 4 已实现：蓄势模组的 3 个专属强化 + 6 条进化**

- **精准释放**（`ch_release`）：`updateChargedAttack` 在松开左键时先判定
  `chargeT` 是否落在 `[chargePreciseMin, chargePreciseMax]` 窗口内——**窗口下限
  可以低于 `chargeTime` 本身**，命中窗口就按「满蓄」处理，不需要真的攒够蓄满时间，
  这是它「提前一点点也能打出满蓄斩」的意义所在。基础窗口 0.55–0.68s，伤害
  `dmg×2.8`、后摇 `atkCd×1.3`（覆盖普通满蓄的 `chargeDamageMult`/
  `chargeRecoverMult`，不是叠加）。完美时机把窗口收窄到 0.58–0.64s、伤害提到
  `×3.6`，命中 ≥2 个敌人额外减冲刺冷却 0.30s；宽容节拍把窗口放宽到 0.48–0.78s、
  伤害 `×3.0`，一个敌人都没打中时后摇再打对折。`drawChargeRing` 在窗口内把蓄力环
  变成白色高亮，作为「现在松手」的读秒提示。
- **震荡**（`ch_shock`）：`Enemy` 新增 `stunT`（完全冻结用，衰减在
  `updateEnemyCommon`，消费在 `enemies/index.ts` 的 `updateEnemy` 分发处——
  `stunT>0` 时整个跳过该敌人的 update，连冷却都不倒计时）。新增
  `World.interruptEnemy(e)`：只打断非 Boss 且 `state==='telegraph'` 的敌人，
  重新抽一个正常冷却（不能只把 `state` 改回 `idle`——敌人的 `cd` 在预警期间没被
  动过，原样放回去下一帧会立刻又触发同一个预警，所以打断必须重新抽冷却才有意义）。
  蓄力斩命中时在 `player.ts` 的 `slash()` 里两阶段结算：先对所有会命中的敌人尝试
  打断并计数，再统一结算伤害——反震的加成必须按「这一整下蓄力斩」的总打断数算，
  不能按命中顺序临时累加。封招：打断的敌人额外 `stunT=1.2` + 冷却 `×1.4`；反震：
  每打断一个敌人，本次蓄力斩所有命中都加 `dmg×0.35` 伤害，最多按 3 层算。
- **余震**（`ch_after`）：新增 `World.spawnAftershock(x, y, radius)`，蓄力斩命中
  判定完之后在玩家当前位置排一个 `timeline.after(aftershockDelay, …)`，到点对
  半径内敌人造成 `dmg×aftershockDamageMult`（`AFTEREFFECT` 标签）。二重余震额外
  排第二段（0.75s 时 `dmg×0.85`，第一段降到 `dmg×0.55`）；扩散余震把半径提到
  `×1.35`（相对蓄力斩命中范围）并加一次强击退。
- 三条递归禁令目前全部落地为同一种模式：`spawnAftershock`/`spawnAfterimage`/
  `detonateMarks` 都只在各自唯一的触发点调度，从不在 `damageEnemy` 内部反向调度，
  所以 `AFTEREFFECT`/`DASH` 起的爆炸/余震永远不可能连锁触发新的自己——禁令是
  结构性的，靠"没有代码路径能从伤害走回调度函数"，不需要额外的状态位或递归深度
  计数。`DamageTag` 六个标签（M1 打地基）到这里全部被至少一种真实机制消费。
- **已知 bug 修复**（写测试时抓到，和 M3 是同一类问题）：`dashCd` 在蓄力攒够
  0.60s 的整个过程中一直在自然递减（`updatePlayer` 每帧无条件 `p.dashCd -= dt`），
  完美时机测试最初直接断言 `1.0 - 0.30` 会失败——蓄力那 0.6s 本身也吃掉了将近
  0.6s 冷却。修好后用「跑一样长的蓄力，附近没有敌人所以不触发额外返还」的对照组
  取差值，把自然递减这个共同项消掉。另有一个真实几何 bug：测试里手动把敌人塞进
  `telegraph` 状态却没给 `t` 赋一个够大的值时，冲锋兵的 telegraph 会在蓄力攒够
  之前自己倒计时归零、切进 `attack` 状态并带着固定冲刺速度飞走——蓄力斩落地时
  它早就不在原地了。这提醒了一件事：强行摆敌人状态做测试装置时，必须连带看它
  这个状态本身会不会自然过期，不能只看"我需要它现在是什么状态"。
- 新测试文件 `tests/chargeModule.test.ts`（19 项）：精准释放窗口内/窗口下限低于
  chargeTime 也算/窗口外退化为普通挥砍、完美时机的收窄窗口与命中减冷却、宽容节拍
  未命中减半后摇；震荡对四种敌人（冲锋兵/射手/重甲/医疗兵）逐一验证能打断、Boss
  不能打断、非预警状态打不断、封招的硬直与冷却延长、硬直期间完全冻结、反震的按
  打断数加伤；余震的单段延迟起爆、二重余震两段数值、扩散余震的半径与击退、
  AFTEREFFECT 不会再排出新余震的递归禁令。`combatLoop.test.ts` 加了一条蓄势三
  强化（含封招 + 二重余震）在真实 Boss 房打满 2400 步的报价一致性 fuzz。

**里程碑 3 已实现：掠影模组的 3 个专属强化 + 6 条进化**

- **连闪**（`da_flash`）：`Player` 新增 `dashFlashReduced`（本次冲刺已减掉的冷却，
  用来在 cap 前停手）。每冲刺穿过一个新敌人（`applyPhantomStrike` 里），冲刺冷却
  实时 -0.15s，单次冲刺封顶 -0.45s。无间把封顶提到 0.75s，命中 ≥3 个敌人时
  `finishDash`（冲刺结束那一帧）额外给 0.15s 无敌。精准闪避：冲刺起跳那一刻判定
  一次「险境」（`isNearMiss`——敌方弹预计命中时间 ≤0.18s，或冲锋兵正处于冲刺攻击
  状态；简化实现，不逐个敌人精确读秒，见下面的已知简化），命中就在冲刺结束时额外
  返还 55% 冷却。
- **破阵**（`da_break`）：`Enemy` 新增 `brokenT`（衰减逻辑在 `updateEnemyCommon`，
  和 `markT` 同一处）。冲刺穿过的敌人进入易伤：基础只放大 MELEE 伤害（+20%，Boss
  +12%），碎甲（`breakAll=true`）放大所有伤害来源（+30%，Boss +18%），持续时间两
  条分支都不变（1.6s / Boss 1.0s，写死在 `MODULES.dash`，不是 Stats 字段）。追杀：
  击杀还处于破阵状态的敌人时，在 `damageEnemy` 的死亡分支里直接给玩家减冲刺冷却
  （-0.35s）+ 临时加速（`Player.speedBuffT`，+20% 移速持续 1.5s，`updateMovement`
  里和蓄势的蓄力减速用同一个乘数位置，不叠加只刷新）。
- **残影**（`da_ghost`）：新增 `World.spawnAfterimage(x, y)`，冲刺结束时
  （`finishDash`）在冲刺起点埋一个 `timeline.after(ghostDelay, …)` 定时炸弹，到点
  对半径 75px 内敌人造成 `dmg × ghostDamageMult`（`AFTEREFFECT` 标签）。双生残影
  在起点和终点各埋一个（各 65% 伤害，同一敌人可以分别中两次）；延迟猎杀把延迟提到
  0.75s、伤害提到 135%，起爆前有一个持续显示的警示环（`Fx.ring` 的 life 设成整个
  等待时长）。
- 又一条递归禁令落地：`spawnAfterimage` 的爆炸只在 `finishDash` 里调度，从不在
  `damageEnemy` 内部调度——所以 `AFTEREFFECT` 标签的伤害不可能再生成新的残影，
  禁令是结构性的（没有任何代码路径能从爆炸伤害走回调度函数），不需要额外状态位，
  和爆印的 `EXPLOSION` 禁令是同一种做法。
- **已知简化**：精准闪避的「险境」判定没有对每种敌人预警精确读秒到 0.18s，只覆盖
  两类最常见、最好读的威胁（敌方弹的飞行时间、冲锋兵的冲刺攻击状态）。重甲的爆发
  预警、Boss 的招式预警目前不会触发精准闪避。这是刻意的范围收窄，不是遗漏——完整
  实现需要给每种敌人的每个预警状态建一套「还剩多久命中」的模型，复杂度和收益不成
  比例；等实际试玩觉得有必要再补。
- **已知 bug 修复**（写测试时抓到）：冲刺命中敌人会触发顿帧（`addHitstop`），顿帧
  期间世界按 `hitstopScale`（0.12）慢放——这**也会让冲刺自己的倒计时变慢**。密集
  穿过多个敌人的一次冲刺，真实经过的步数会远超 `dashTime / FIXED_STEP` 的朴素估算
  （连续触发顿帧时可能慢十倍以上）。这是游戏本身的真实行为（顿帧理应影响冲刺本身），
  但第一版测试按朴素步数算漏了这一点，导致「无间额外无敌」断言失败；修好后测试改成
  驱动到 `dashT` 真正归零为止，不再猜步数。
- 新测试文件 `tests/dashModule.test.ts`（14 项）：连闪冷却封顶（用「有连闪/没有
  连闪」两组跑一样的冲刺，差值消掉冷却的自然递减）、无间额外无敌、精准闪避的险境
  判定与返还、破阵的 MELEE-only vs 全伤害倍率、Boss 版本倍率、debuff 过期、追杀的
  击杀触发、残影的延迟起爆/双生双点/延迟猎杀数值/递归禁令。`combatLoop.test.ts`
  加了一条掠影三强化（含精准闪避 + 双生残影）在真实 Boss 房打满 2400 步的报价一致
  性 fuzz。

**里程碑 2 已实现：飞刃模组的 3 个专属强化 + 6 条进化**

- **贯刃**（`bl_pierce`）：`Bullet` 新增 `pierceMode`（`'stack'`=无阻式层层衰减 /
  `'finale'`=贯心式二段终结）、`pierceLeft`/`pierceFalloff`/`pierceBonus`、
  `hitEnemies`（去重）、`legHits`（当前段命中数）。基础穿 1 个、伤害降到 75%；
  无阻穿 3 个、伤害每次只降 15%（复合衰减，即第 N 次命中伤害 = 首次伤害 ×
  falloff^(N−1)）；贯心固定命中 2 个，第二下 +80% 伤害后立即消失。
- **回旋**（`bl_return`）：`World.stepPlayerBlade` 把子弹更新拆成 out/back/orbit
  三段状态机（`turnBladeBack`/`finishBladeReturn`）。基础版到达最大距离
  （`MODULES.blade.speed × life`）或撞墙就转身飞回玩家，去程和回程各能命中同一个
  敌人一次（`hitEnemies` 在每段开始时清空）；归刃提升回程速度到 820px/s 并给回程
  命中 ×1.35 伤害；环身让子弹回到玩家身边后环绕 0.8s，环绕期间命中范围内每个敌人
  各一次。
- **刃印**（`bl_mark`）：`Enemy` 新增 `markStacks`/`markT`（层数/剩余时间，衰减逻辑
  在 `updateEnemyCommon`）。`World.damageEnemy` 现在按 `tag` 分支处理：`BLADE`
  命中叠一层（封顶后刷新持续时间，不增加层数）；`MELEE` 命中消耗全部层数，按
  `dmg × 0.20 × 层数` 追加伤害。猎印把层数上限提到 5，并给 MELEE 命中加
  `×(1+0.05×层数)` 的乘算倍率（叠加原有的加算伤害，不是互斥）；爆印让层数叠满时
  自动引爆（`World.detonateMarks`，`EXPLOSION` 标签），本体 90% 伤害 + 90px 内
  溅射 45% 伤害，之后不再靠挥砍消耗。
- 递归禁令落地一条：`detonateMarks` 用 `EXPLOSION` 标签递归调用 `damageEnemy`，
  而叠层/引爆逻辑只认 `tag==='BLADE'`，所以爆炸伤害不会再触发新的刃印——不需要
  额外的「已经在引爆中」标记，标签本身就是禁令。
- `Enemy.lastHitTag` 机制（M1 打的地基）第一次被真正消费。
- 新测试文件 `tests/bladeModule.test.ts`（11 项）：穿透衰减/复合衰减/贯心终结、
  回旋去回各命中一次/归刃加成/环身环绕命中、刃印叠层封顶/过期/消耗加伤/猎印乘算/
  爆印引爆与溅射/EXPLOSION 不叠印的递归禁令。`combatLoop.test.ts` 加了一条飞刃
  三强化（含环身进化）在真实 Boss 房打满 2400 步的报价一致性 fuzz。
- 已知简化：贯刃的伤害衰减顺序按「本次命中先按当前倍率结算，再为下一次命中打折」
  实现（首次命中永远是满伤害，这是实现时抓到的一个真实 bug——最初写成了"命中次数
  >1 才打折"，落地成了第二下才打折但用的还是满伤害，被新测试当场抓出来）。

**里程碑 1 已实现：**

- 开局流程：标题页填 seed → **模组选择页**（飞刃/掠影/蓄势，三选一，不计时，选定后
  不可更改）→ 地图。`SceneContext.startRun(seed, module)` 签名已改，`toModuleSelect`
  新增。
- 三个模组的**基础能力**从选中那一刻起就生效，不需要拿任何强化：飞刃（挥砍带刃弹，
  `dmg×0.40`）、掠影（冲刺穿人 `dmg×0.85`）、蓄势（左键变蓄力键，0.60s 蓄满，
  `dmg×2.2` 全向斩，蓄力中移速 ×0.72）。数值集中在 `src/game/config.ts` 的
  `MODULES`，写入 Stats 的逻辑在 `src/game/modules.ts`。
- 强化模型重写（`src/game/upgrades.ts`）：`Upgrade.module: ModuleId|'universal'`；
  `Evolution.branch: 'a'|'b'`，`Run.evolved: Map<UpgradeId, 'a'|'b'>`。**分支保留
  直到被选中**——未进化的强化两条分支都可能反复出现在奖励/商店里，选中一条后整个
  强化才完成进化、两条分支才一起永久离场（v2「展示即淘汰」已废除）。
- 4 个通用强化（疾风/利刃/韧体/精算）+ 8 条进化，加上飞刃/掠影/蓄势三个模组各 3
  个专属强化 + 6 条进化（合计 13 个基础强化 + 26 条进化），全部按 DESIGN.md
  §3/§4/§5/§6 的数值实现（细节见下面的「里程碑 2/3/4」小节）——第一版规模
  （DESIGN.md §14）到此**全部实现完**。裂刃/距离校准（飞刃）、折返/终点斩
  （掠影）、聚势/收割（蓄势）这 6 个「扩展范围」强化不在第一版范围内，暂不计划。
- 奖励/商店生成规则重写（新文件 `src/game/rewards.ts`）：战斗房 3 选 1（2 模组专属
  + 1 通用，35% 概率换成进化）、精英房 3 选 1（尽量多给进化，没有可进化强化就退化
  成 3 个模组专属）、商店 4 槽（模组基础/模组基础或进化/通用或进化/随机折扣）。
  防无效选项 6 条规则（DESIGN.md §2.4）在这个文件里集中实现。三个模组的专属强化池
  现在都不是空的，选任意模组商店/奖励都能正常抽到对应的三个专属强化。
- 伤害标签：`World.damageEnemy(e, damage, tag)` 的 `DamageTag` 参数
  （`MELEE|BLADE|DASH|CHARGE|EXPLOSION|AFTEREFFECT`）从 M1 打地基，到 M4 结束时
  六个标签全部被至少一种真实机制消费：`BLADE`/`MELEE` 驱动刃印，`EXPLOSION` 是
  爆印的引爆，`DASH` 驱动破阵，`CHARGE` 驱动震荡打断，`AFTEREFFECT` 是残影和余震
  共用的爆炸标签。
- 精算的「高额结算」进化（精英房清空额外返还）在 `CombatScene` 里按房型触发一次，
  不随击杀次数重复结算。
- 删除的旧机制：处决（`exec`）、时停（`dashSlow`/`applySlow`/`World.slow`）、反击
  （`counterDmg`/`Player.counter`/`HIT.riposteWindow`）—— 对应 DESIGN.md §7 的
  「删除的旧强化」表。`FEEL.slowScale` 常量保留，只作为 Timeline 不变式测试的通用
  减速倍率，和已删除的时停机制无关。
- 8 层分支地图，首层战斗、倒数第二层休整、最终 Boss（v2 遗留，本次未改）。
- 房间类型：普通战斗、精英、商店、时间修复站、捷径门、Boss（v2 遗留）。
- 战斗：移动、鼠标瞄准、近战、冲刺无敌、受击硬直、连击税、墙体碰撞（v2 遗留）。
- 敌人：冲锋兵、射手、重甲、医疗兵；两阶段 Boss（冲锋反向折返、弹幕缺口轮转、震波
  由外向内回收，回收动作结束后短暂露出高伤害破绽）（v2 遗留）。
- 全局 seed：地图、房间、敌人出生点、强化/进化抽取和 Boss 招式均可复现。
- 中英文 UI 与本地语言记忆；`src/i18n/strings.ts` 已加 `modules`/`moduleSelect`
  两个新词典分区。
- 暂停为全遮挡式暂停；地图、模组选择页除外的其余决策界面正常计时。
- 走表规则：玩家能操作或作决定时时间才计入成绩。房间清空后的 0.45s 过场（纯动画）
  停表；顿帧期间账本按世界速度（×0.12）打折。受击僵直不停表（惩罚本身）。场景通过
  `Scene.timeScale` 报价，App 主循环按它缩放走表；`tests/timeAccounting.test.ts`
  钉住这两条规则和两个反例。
- 精英房数值：重甲数量 `1+floor(层数/3)`，血量倍率 ×1.2；`tests/eliteBalance.test.ts`
  钉住，不受本次模组重构影响。

## 已验证

2026-08-04 已执行并通过：

```text
npm test       # 15 个测试文件，156 项测试全部通过
npx tsc --noEmit  # 类型检查通过
bash tests/run.sh  # 推送闸门（类型检查 + 测试 + 生产构建）全部通过
```

现有自动化覆盖：定价一致性、随机性/seed 可复现、游戏时间轴、地图约束、医疗兵行为、
精英房数量护栏、Boss 回收阶段、强化/进化模型（分支保留直到被选中、精英房不重复抽
同一强化的两条分支、35% 战斗房进化替换概率、模组基础能力生效）、飞刃模组三专属
强化的贯刃/回旋/刃印全部机制（`tests/bladeModule.test.ts`）、掠影模组三专属强化
的连闪/破阵/残影全部机制（`tests/dashModule.test.ts`）、**蓄势模组三专属强化的
精准释放/震荡/余震全部机制（`tests/chargeModule.test.ts`）**、战斗循环（三模组各
跑一遍 fuzz，另加飞刃/掠影/蓄势满强化各一条在真实 Boss 房的报价一致性 fuzz，共
覆盖 13 个专属强化中的绝大多数）、走表规则、i18n、启动冒烟测试（含模组选择页流程）。

手动验证：`bash tests/run.sh` 内建的 jsdom 冒烟测试已经把「标题页填 seed → 选模组
→ 进地图 → 打战斗房 → 领奖励 → 逛商店 → 打 Boss → 结算页」整条链路走了一遍并断言
DOM 状态，覆盖了比单纯类型检查更贴近真实交互的路径；没有额外用浏览器手动点过。

## 发布约定

- 每次完成并验证代码或文档改动后，必须提交并推送到 GitHub 的 `main` 分支；不要只保留本地改动。
- 每次改动完，都要在这份文件里更新对应内容，让 Codex 和 Claude Code 看到的现状是一致的。
- `tests/` 已经**整个进仓库**，以后新增测试文件正常 `git add` 即可；CI
  （`.github/workflows/deploy.yml`）跑的就是 `bash tests/run.sh` 本地闸门那一整套。

## 关键文件索引

| 目的 | 文件 |
| --- | --- |
| 目标玩法规格（v3） | `DESIGN.md` |
| 当前协作状态 | `PROJECT_STATUS.md` |
| 全部数值配置（含 `MODULES`/`PRICES`/`REWARDS`） | `src/game/config.ts` |
| 模组基础能力 | `src/game/modules.ts` |
| 强化/进化数据与 computeStats | `src/game/upgrades.ts` |
| 奖励/商店生成规则 | `src/game/rewards.ts` |
| 一局状态、seed 分流、进化归属 | `src/game/run.ts` |
| 时间账本与总成绩 | `src/game/ledger.ts` |
| 地图生成和路线约束 | `src/game/map.ts` |
| 房间敌人编排 | `src/game/room.ts` |
| 战斗世界与受击/伤害/子弹状态机（含 DamageTag、刃印结算） | `src/game/world.ts` |
| 各场景流转（含模组选择页） | `src/scenes/` |
| 测试 | `tests/` |
