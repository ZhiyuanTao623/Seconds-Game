# 秒 · SECONDS — 玩法设计文档

> **这份文档的用法**
> 下面是当前 `index.html` 原型里**所有**玩法相关的内容，逐条从代码里抽出来的，数值与现版本一致。
> 你直接在这份文档上改：改数字、划掉不要的、写新的。改完我按这份文档重新实现。
> 每节末尾的 `> 💬 备注：` 是留给你写想法的地方。
>
> 标注说明：
> `⚠️ 疑似 bug` = 当前行为可能不是有意设计的，需要你裁决是保留还是修掉。
> `🎯 设计意图` = 从代码注释和 README 里读出来的原始意图。

---

## 0. 一句话核心

**唯一的货币是"秒"。你没有血量。被打中不会死，只会往计时器上加秒数。**

买强化花秒、开捷径花秒、修复伤势花秒。而 Boss 的强度 = 你累计的总时间。

🎯 设计意图（来自 README）：验证"玩家能不能不断提高操作水平来让游戏时间越来越短，同时把每次受击也算作时间惩罚"。每个决定都是同一个问题：**这值几秒？**

> 💬 备注：

---

## 1. 时间账本（核心经济）

### 1.1 总时间公式

```
总时间 = 游戏时间(tPlay) + 受击惩罚(tPen) + 秒数消费(tSpend) − 击杀返还(tRef)
```

| 项 | 来源 | 方向 |
|---|---|---|
| `tPlay` 游戏时间 | 只在 `scene === 'play'` 时按真实时间累加（受 hitstop 影响后的 dt） | 增 |
| `tPen` 受击惩罚 | 每次被打中，按报价公式加 | 增 |
| `tSpend` 秒数消费 | 商店买强化、开捷径、修复站 | 增 |
| `tRef` 击杀返还 | 只有「精算」强化提供，每杀一个非 Boss 敌人 | 减 |

⚠️ 疑似 bug：菜单、地图、商店、结算界面**不计时**，只有战斗房内计时。也就是说站在地图界面思考是完全免费的。这削弱了"时间是稀缺资源"的压迫感——你可以无限思考。**要不要给决策界面也计时（或计半速）？**

### 1.2 难度随总时间缩放

| 名称 | 公式 | 现值示例 |
|---|---|---|
| Boss 强度 `bossScale` | `1 + 总时间 / 150` | 150s → ×2.00；300s → ×3.00 |
| 杂兵强度 `enemyScale` | `1 + 总时间 / 400` | 150s → ×1.375；300s → ×1.75 |

**缩放只影响血量，不影响伤害（时间惩罚）和移速。**
唯一例外：Boss 移速 = `78 × (1 + (bossScale − 1) × 0.25)`。

⚠️ **死亡螺旋**：受击 → 总时间涨 → 敌人血更厚 → 打得更久 → 游戏时间又涨 → 敌人更厚。菜的人会被指数级惩罚。这是有意的吗？

> 💬 备注：

### 1.3 结算评级

| 评级 | 总时间 |
|---|---|
| S | < 150s |
| A | < 195s |
| B | < 245s |
| C | < 310s |
| D | ≥ 310s |

> 💬 备注：

---

## 2. 受击经济学（连击税）

这是这个游戏的**核心不变式所在**：屏幕上任何一个 `+X.Xs` 价签，必须和实际扣的秒数逐字相同。

### 2.1 报价公式

```
本次惩罚 = 底价(base) × 惩罚倍率(penMult) × 连击税(comboTax)
连击税  = 1.3 ^ 连击层数(streak)
```

**若玩家处于无敌（受击无敌 or 冲刺中），报价 = 0，且实际不扣钱。**（"免费"绿字）

### 2.2 参数

| 参数 | 现值 | 含义 |
|---|---|---|
| `INV_TIME` 受击无敌 | **0.40s** | 挨打后的无敌时长 |
| 冲刺无敌 | **0.17s** | 整个冲刺过程完全无敌 |
| `TAX_WINDOW` 税窗口 | **5.0s** | 距上次受击多久内继续累进；超时归零 |
| `TAX_STEP` 税率 | **×1.3** | 每多挨一次，价码乘 1.3 |
| 受击后反击窗口 `P.counter` | 2.0s | 只有配了「反击」强化才有意义 |

### 2.3 连击税实例（以冲锋兵 2.0s 底价为例）

| 第几下 | 倍率 | 实扣 |
|---|---|---|
| 1 | ×1.00 | 2.0s |
| 2 | ×1.30 | 2.6s |
| 3 | ×1.69 | 3.4s |
| 4 | ×2.20 | 4.4s |
| 5 | ×2.86 | 5.7s |

🎯 设计意图：无敌帧本质是一笔"退款"——挨一下 = 花 pen 秒买 0.4 秒的穿墙权。0.4s 压得刚好够脱离接触，太长会让"无脑硬吃"变成最优解。连击税就是防止玩家把无敌帧当免费穿墙权用。

> 💬 备注：

---

## 3. 玩家角色

### 3.1 基础数值 `BASE`

| 参数 | 现值 | 说明 |
|---|---|---|
| `dmg` 伤害 | 12 | 每次挥砍 |
| `atkCd` 攻击间隔 | 0.36s | |
| `range` 攻击范围 | 78px | |
| `arc` 挥砍弧度 | 1.9 rad ≈ 109° | 扇形张角 |
| `spd` 移速 | 268 px/s | |
| `dashCd` 冲刺冷却 | 1.05s | |
| `penMult` 惩罚倍率 | ×1 | |
| `costMult` 消费倍率 | ×1 | |
| `exec` 处决阈值 | 0（关闭） | |
| `refund` 击杀返还 | 0s | |
| `dashSlow` 时停时长 | 0（关闭） | |
| `counterDmg` 反击加伤 | 0（关闭） | |
| 碰撞半径 `r` | 13px | |

### 3.2 冲刺

| 参数 | 现值 |
|---|---|
| 持续 | 0.17s |
| 速度 | 940 px/s（总位移约 160px） |
| 无敌 | **全程** |
| 方向 | **只看光标，与移动键无关**，按下那一帧锁死，途中不能转向 |
| 触发键 | `空格` / 鼠标右键 |
| 视觉 | 角色周围蓝环 = 冷却进度 |

🎯 设计意图：冲刺方向锁死+只看光标，是为了让"往哪躲"变成一个必须提前算好的决定，而不是可以中途反悔的操作。想往身后躲就把光标甩到身后。

### 3.3 挥砍

| 参数 | 现值 |
|---|---|
| 触发 | 鼠标左键（按住可连续） |
| 判定 | 距离 < `range + 敌人半径`，且角度差 < `arc / 2` |
| 前冲 | 每次挥砍角色沿瞄准方向瞬移 **14px** |
| 命中效果 | 敌人击退 180 px/s（摩擦系数 0.002^dt），hitstop 0.03s |
| 打飞行弹 | 可以打掉飞来的红弹 |

⚠️ 疑似 bug：**打子弹的判定不看角度**，只判 `距离 < range × 0.7`（≈55px）。也就是说挥砍会同时打掉身后的子弹。要保留这个"全向弹幕清除"还是改成和打敌人一样的扇形判定？

### 3.4 受击反馈

| 效果 | 数值 |
|---|---|
| 击退自身 | 260 px/s（沿远离伤害源方向） |
| 屏幕震动 | 16 |
| 顿帧 hitstop | 0.07s（期间 dt × 0.12） |
| 红闪 | 0.35s |
| 移动摩擦 | v × 0.0016^dt |

> 💬 备注：

---

## 4. 敌人

**通用规则**：`enemyScale` 和精英房倍率**只乘血量**，不影响时间惩罚和移速。

### 4.1 冲锋兵 `charger`（三角形）

| 参数 | 现值 |
|---|---|
| 血量 | 32 |
| 半径 | 15 |
| **时间惩罚** | **+2.0s** |
| 移速 | 96 px/s |

**行为循环**

1. `idle` — 直线追击玩家（距离 > 40 才动）。初始冷却 `rnd(0.3, 1.4)`
2. 触发条件：距离 < 400 且冷却好了 且 **视线未被墙挡住** → 进入预警
3. `tel` 预警 **0.62s** — 画红色锁定射线（长 330px），**方向在预警开始那一帧就锁死**
4. `dash` 冲刺 **0.40s** @ **760 px/s**（约 300px）— 接触判定持续生效
5. `rec` 硬直 **0.55s** → 回 idle，冷却 `rnd(0.5, 1.1)`

🎯 破解方式：方向预警时就锁死，侧移一步即可。

### 4.2 射手 `shooter`（方块）

| 参数 | 现值 |
|---|---|
| 血量 | 24 |
| 半径 | 13 |
| **时间惩罚（子弹）** | **+1.5s** |
| 移速 | 118 px/s |

**行为循环**

1. `idle` — 走位：距离 < 200 后退 / 距离 > 330 靠近 / 中间距离绕圈（垂直于玩家方向）
2. 触发：冷却好 且 距离 < 460 且 视线不被挡 → 预警
3. `tel` 预警 **0.48s** — 红色**虚线**锁定射线（长 520px），方向锁死
4. 发射：1 发红弹 → 回 idle，冷却 `rnd(1.8, 2.6)`

**子弹**：半径 6，速度 350 px/s，存活 3s，撞墙消失，可被挥砍打掉

### 4.3 重甲 `brute`（六边形）

| 参数 | 现值 |
|---|---|
| 血量 | 78 |
| 半径 | 24 |
| **时间惩罚** | **+4.0s** |
| 移速 | 64 px/s |

**行为循环**

1. `idle` — 追击（距离 > 78 才动）
2. 触发：距离 < 130 且冷却好 → 预警（**不检查视线**）
3. `tel` 预警 **0.95s** — 脚下红圈从 0 向外扩到半径 132
4. 爆发：**扩满瞬间**，半径 132 内**全圈**伤害
5. `rec` 硬直 **0.8s** → 冷却 `rnd(0.8, 1.6)`

### 4.4 BOSS（八边形）

| 参数 | 现值 |
|---|---|
| 血量 | `420 × bossScale` |
| 半径 | 42 |
| 移速 | `78 × (1 + (bossScale−1) × 0.25)` |
| **接触伤害** | **+5.0s** |

**招式选择**（每次 idle 结束时随机，初始冷却 1.2s）

| 概率 | 招式 |
|---|---|
| 38% | 环形弹幕 `telBurst` |
| 34% | 三连冲锋 `telCharge` |
| 28% | 三段震波 `telSlam` |

idle 时：距离 > 200 才追击。

#### 招式 A · 环形弹幕（**+2.5s**）

- 预警 **0.8s** — 身周画一个向内收缩的圆
- 发射 **16 发**均匀环形弹，起始角度随机
- 弹速 280 px/s，半径 7，存活 4s
- 后摇冷却 1.0s，震屏 12

#### 招式 B · 三连冲锋（**+5.0s** 接触）

- 预警 **0.7s** — 粗红线指示方向（长 700px），方向锁死
- 冲锋 **0.5s** @ **700 px/s**（约 350px）
- **连续 3 次**，第 2、3 次预警缩短到 **0.38s** 且**重新锁定玩家当前位置**
- 结束冷却 1.3s

#### 招式 C · 三段震波（**+3.0s**）

- 预警 **1.0s** — 三个同心圆同时向外扩
- 落地：三道环依次生成，半径 **90 / 175 / 260**，间隔 **190ms**
- 判定带：距 Boss 中心 `[半径−66, 半径+26]` 范围内命中（宽约 92px）
- 后摇冷却 1.1s，震屏 16

⚠️ 严重问题：三段震波用**真实时间**（`setTimeout`）计时，与游戏时钟脱钩。开「时停」强化或触发顿帧时，视觉预警和实际判定会错位。重构必须改成游戏时钟驱动。

⚠️ 只有**一个** Boss，三招循环，无阶段变化、无血量阶段。这是重做的重点吗？

> 💬 备注：

---

## 5. 强化（共 10 个）

**同一局内不重复**（已持有的不再出现）。价格是商店价，战斗房掉落免费。

| 名称 | 商店价 | 效果 | 收益/代价 |
|---|---|---|---|
| **利刃** | 14s | 伤害 ×1.6，受击惩罚 ×1.4 | 打得快，挨打更贵 |
| **疾风** | 16s | 移速 ×1.16，冲刺冷却 ×0.65 | 纯机动 |
| **韧体** | 15s | 受击惩罚 ×0.65，伤害 ×0.85 | 挨打便宜，打得慢 |
| **长刃** | 12s | 攻击范围 ×1.45，弧度 ×1.15 | 纯攻击距离 |
| **连斩** | 14s | 攻击间隔 ×0.6，伤害 ×0.9 | DPS ×1.5 |
| **处决** | 20s | 敌人血量 < 30% 时被斩击**立即斩杀**（Boss 无效） | 省收尾时间 |
| **贪婪** | 13s | 此后所有秒数消费 ×0.65 | 越早拿越值 |
| **时停** | 18s | 冲刺时全场减速 60%，持续 0.55s | 救命 + 输出窗口 |
| **反击** | 11s | 受击后 2 秒内伤害 +85% | 奖励主动挨打？ |
| **精算** | 15s | 每击杀一个敌人返还 **0.5s**（Boss 无效） | 直接减总时间 |

⚠️ 数量问题：一局最多拿到 ~5–8 个（7 节点 × 战斗房掉落 + 商店购买），但池子只有 10 个。**重玩性极低**，第二局基本就见全了。

⚠️ 「反击」的触发条件是"受击后"，但受击是要付秒数的——这个强化在鼓励玩家做违背核心目标的事。是有意的博弈还是设计失误？

⚠️ 全是被动数值修正，**没有一个改变操作方式**（比如加一个第二种攻击、改变冲刺行为）。

> 💬 备注：

---

## 6. 关卡结构

### 6.1 整体

**一章 = 7 个节点 + 最终 Boss。** 线性推进，无分支地图。

### 6.2 房间类型与出现规则

每次通过节点后，给你 2–3 张明码标价的路线卡：

| 卡片 | 出现概率 | 代价 | 收益 |
|---|---|---|---|
| **战斗房** | 100%（必出） | 战斗消耗的游戏时间 | 清空后 **2 选 1** 免费强化 |
| **精英房** | 75% | 更长的战斗时间 | 清空后 **3 选 1** 免费强化 |
| **捷径门** | 45%（三选一） | **9s** × costMult | 跳过一个节点，少打一场也少拿一个强化 |
| **时间修复站** | 35%（三选一） | **12s** × costMult | 消除已累计 `tPen` 的 **40%** |
| **秒 · 商店** | 20%（三选一） | 各强化明码标价 | 可买多个，永远买得起 |

**第三张卡是三选一**：45% 捷径门 / 35% 修复站 / 20% 商店。

界面上显示的"预计游戏时间"：`est = 8 + 节点数 × 3`
- 战斗房：`est ~ est+10` 秒
- 精英房：`est+10 ~ est+26` 秒

### 6.3 敌人配置

| 房间 | 敌人组成 | 血量倍率 |
|---|---|---|
| **战斗房** | `2 + 节点数` 个（⅔ 冲锋兵 / ⅓ 射手）；节点 ≥ 3 时额外 +1 重甲 | ×1（仅 enemyScale） |
| **精英房** | `2 + floor(节点数/2)` 个重甲 + `2 + 节点数` 个（冲锋兵/射手各半） | **×1.35** + enemyScale |

节点 7 的战斗房 = 9 个杂兵 + 1 重甲。精英房 = 5 重甲 + 9 杂兵。

⚠️ 只有"数量堆叠"这一种难度曲线，敌人种类从头到尾就 3 种。

### 6.4 商店细节

- 一次开出 3 个强化，买走后该位置留空（不补货）
- 价格 = `max(3, round(基础价 × costMult))`
- 可以全买，也可以直接离开
- **永远买得起**——因为代价直接记在成绩上，不存在"钱不够"

🎯 设计意图：取消"买不起"这个概念，让每次购买都是纯粹的价值判断，而不是资源管理。

> 💬 备注：

---

## 7. 场地

- **尺寸**：1000 × 620（固定，等比缩放到窗口）
- **墙体布局**：进房间时从 5 种预设里随机抽 1 种

| # | 布局 |
|---|---|
| 1 | 左上、右上两个方块 + 中下一个横块 |
| 2 | 左右两根竖长条 |
| 3 | 上下两根横条 + 左右两个方块 |
| 4 | 四角各一个方块 |
| 5 | **空场**（无掩体） |

- **玩家出生点**：(500, 530)，屏幕正下方
- **敌人生成**：x ∈ [80, 920]，y ∈ [60, 400]，且距玩家 > 240px（最多重试 40 次）
- 墙体会阻挡冲锋兵/射手的视线（不阻挡重甲和 Boss）
- 子弹撞墙消失

⚠️ 布局是随机抽的，与房间类型/难度无关。空场（第 5 种）配 5 个重甲会非常难受。

> 💬 备注：

---

## 8. 报价 UI（核心表达）

**场上每一个会收你钱的东西都挂着实时价签**，上面的数字就是你此刻挨这一下会被扣的秒数，已经算进强化和连击税。

| 状态 | 颜色 | 显示 |
|---|---|---|
| 常态 | 红色 | `+2.0s` |
| 连击税生效中 | **橙色加粗** | `+2.6s` |
| 玩家无敌中 | 绿色 | `免费` |

- **敌人**：脚下 10px 字号价签，恒定显示
- **子弹**：9px 字号，透明度 = `clamp(1 − (距玩家 − 100) / 200, 0, 1)`，越靠近越清楚（防止 Boss 弹幕糊成一片）
- **Boss 分招式报价**：起手三层环 → `+3.0s`；起手收缩圆 → `+2.5s`；冲锋接触 → `+5.0s`

🎯 这是整个游戏最核心的表达。**必须保住的不变式**：屏幕显示的秒数 === 实际扣的秒数。现有的 [tests/](tests/) 就是专门守这个的（17 项静态断言 + fuzz 循环测试）。

> 💬 备注：

---

## 9. 手感 / 演出参数

| 参数 | 现值 |
|---|---|
| 顿帧 hitstop（受击） | 0.07s，期间 dt × 0.12 |
| 顿帧 hitstop（命中敌人） | 0.03s |
| 屏幕震动（受击） | 16 |
| 屏幕震动（重甲爆发） | 10 |
| 屏幕震动（Boss 弹幕） | 12 |
| 屏幕震动（Boss 震波） | 16 |
| 震动衰减 | × 0.0005^dt |
| 「时停」减速 | 敌人与子弹 dt × 0.4，持续 0.55s |
| 单帧 dt 上限 | 1/25 s（40ms） |
| 房间清空 → 结算界面延迟 | 0.45s |

> 💬 备注：

---

## 10. 操作

| 键位 | 动作 |
|---|---|
| `W A S D` / 方向键 | 移动 |
| 鼠标移动 | 瞄准 |
| 鼠标左键 | 挥砍 |
| `空格` / 鼠标右键 | 朝光标冲刺（全程无敌） |
| `1` `2` `3` `4` | 选卡 |

⚠️ 缺失：**没有暂停键**、没有中途重开、没有手柄支持、没有触屏支持。

> 💬 备注：

---

## 11. 当前设计的整体短板（我的判断）

按重要性排序，供你参考着改：

1. **内容量太薄** — 3 种杂兵 + 1 个 Boss + 10 个强化。第二局就见全了，没有重玩动力。
2. **难度曲线只有一根旋钮** — 全靠"敌人数量 + 血量倍率"堆，没有质变。
3. **强化全是被动数值** — 没有一个改变你怎么操作，构筑之间的差异只体现在数字上。
4. **决策界面不计时** — 削弱了时间压迫感，你可以无限思考。
5. **经济决策不够纠结** — 修复站（12s 换 40% 惩罚）只在 tPen > 30s 时才划算，算一下就有答案，不需要犹豫。捷径门同理。
6. **死亡螺旋无缓冲** — 打得差的人被指数级惩罚，没有翻盘机制。
7. **Boss 无阶段** — 三招随机循环，从头打到尾。
8. **没有元进程** — 单局结束就结束，无解锁、无成长、无排行。

> 💬 你想解决哪几个？打勾或写你自己的想法：

---

## 12. 你想加的新东西

> 💬 在这里随便写，不用管格式。想到什么写什么，我会整理成可实现的规格。





---

## 附：重构后必须保留的技术不变式

无论玩法怎么改，下面这些是我在重构时会强制保证的（跟玩法设计无关，不用你操心）：

1. **报价 === 实扣**：屏幕上任何 `+X.Xs` 与账本扣款走同一条公式、同一个格式化函数，并由测试守住。
2. **所有游戏计时走游戏时钟**，不用 `setTimeout`。暂停、顿帧、时停都能正确影响所有计时器。
3. **逻辑与渲染分离**：渲染函数不修改游戏状态。
4. **数值全部数据化**：所有玩法参数集中在配置文件里，调平衡不用碰逻辑代码。
5. **固定步长物理**：帧率不影响手感与判定结果，且回放可复现。
