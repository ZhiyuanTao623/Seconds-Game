# 秒 · SECONDS — 玩法设计文档 v2

> **这份文档的用法**
> 这是**目标规格**（不是现状记录）。你的批注已经全部并入正文，v1 里被否掉的内容已经删除。
> 继续在上面改：改数字、划掉、写新的。我按这份实现。
>
> 标注说明：
> `🆕` = 本次修订新增或改写的内容
> `🎚️ 待调` = 数值是我给的第一版估计，需要试玩后校准
> `⚠️ 待裁决` = 还需要你做决定的地方
> `🎯 设计意图` = 为什么这么设计

---

## 本次修订摘要

| # | 改动 | 影响范围 |
|---|---|---|
| 1 | **删除所有"难度随累计时间缩放"** — 这是纯竞速游戏 | §1.2 删除、§4.4 Boss 数值重定、§8 HUD、§11 |
| 2 | **决策界面也按 ×1 计时** | §1.1、§6、§10（暂停键成了漏洞） |
| 3 | **无敌期间不显示价签、不扣秒**（原来显示绿色"免费"） | §2.1、§9 |
| 4 | **子弹不可被挥砍打掉** | §3.3、§4.2、§4.4（弹幕可躲性变成硬约束） |
| 5 | **新增受击僵直** | §3.4 |
| 6 | **普通状态下删除反击窗口**（只有持「反击」强化时才存在） | §2.2、§3.4、§5 |
| 7 | **新增改变操作方式的强化「掷刃」** | §5 |
| 8 | **关卡结构改为杀戮尖塔式分支地图 + 可复现 seed** | §6 整节重写、§13 新增技术不变式 |

---

## 0. 一句话核心

**唯一的货币是"秒"。你没有血量。被打中不会死，只会往计时器上加秒数。**

买强化花秒、开捷径花秒、修复伤势花秒。**这是一个纯粹的竞速游戏。**

🎯 设计意图：验证"玩家能不能不断提高操作水平来让游戏时间越来越短，同时把每次受击也算作时间惩罚"。每个决定都是同一个问题：**能不能更快？**

🆕 **删掉时间缩放之后，经济模型反而更干净了。**
原来的核心张力是"变强 → Boss 也变强"，是一个自我平衡的负反馈。现在没有了，取而代之的是一个更直白、也更容易被玩家算清楚的赌注：

> 花 14 秒买这个强化，能不能在剩下的关卡里帮我省回 14 秒以上？

所有消费都是**同一种单位的直接对赌**：秒换秒。没有汇率、没有隐藏成本、没有"买了会变难"的暗税。玩家永远能算，但**算不准**——因为省下多少秒取决于他接下来打得多好。这就是这个游戏想要的决策质感。

> 💬 备注：

---

## 1. 时间账本（核心经济）

### 1.1 总时间公式

```
总时间 = 游戏时间(tPlay) + 受击惩罚(tPen) + 秒数消费(tSpend) − 击杀返还(tRef)
```

| 项 | 来源 | 方向 |
|---|---|---|
| `tPlay` 游戏时间 | 按下方规则实时累加 | 增 |
| `tPen` 受击惩罚 | 每次被打中，按报价公式加 | 增 |
| `tSpend` 秒数消费 | 商店买强化、开捷径、修复站 | 增 |
| `tRef` 击杀返还 | 只有「精算」强化提供，每杀一个非 Boss 敌人 | 减 |

🆕 **哪些界面计时**

| 界面 | 计时 | 说明 |
|---|---|---|
| 战斗房内 | ✅ ×1 | |
| **地图 / 路线选择** | ✅ **×1** | 🆕 |
| **战斗奖励（选强化）** | ✅ **×1** | 🆕 |
| **商店** | ✅ **×1** | 🆕 逛店是要付钱的 |
| 房间清空 → 结算的 0.45s 过场 | ✅ ×1 | |
| 标题菜单 | ❌ | 还没开局 |
| 结算页 | ❌ | 已经停表 |

🎯 设计意图：让"思考"本身也有价格。原来站在地图界面可以无限盘算，时间压迫感在最需要它的地方消失了。现在每一次犹豫都直接写在成绩上——这恰好是这个游戏想让你感受的东西。

⚠️ 待裁决：**这条改动让"暂停键"变成了后门**。见 §10。

### 1.2 ~~难度随总时间缩放~~ 🆕 已删除

**敌人强度与玩家累计时间完全无关。** 血量、移速、时间惩罚全部是固定值。

删掉的内容：`bossScale = 1 + 总时间/150`、`enemyScale = 1 + 总时间/400`、HUD 上的「BOSS 强度 ×N」显示。

保留的非时间缩放：**精英房的 ×1.35 血量倍率**（这是房间类型属性，不是时间函数），以及按层数递增的敌人数量（§6.4）。

🎯 为什么删：竞速游戏里，"打得慢 → 敌人变强 → 打得更慢"是一个指数发散的惩罚，新手会被锁死在下坡路上，而高手几乎感觉不到它存在。它惩罚的是**已经在受苦的人**。而且它让"这个强化值不值"变成一道算不清的题（因为买了会让 Boss 变厚），破坏了 §0 说的那种干净对赌。

### 1.3 结算评级

| 评级 | 总时间 |
|---|---|
| S | < 150s |
| A | < 195s |
| B | < 245s |
| C | < 310s |
| D | ≥ 310s |

🎚️ 待调：**这套分界线现在失准了。** 两个方向的偏移抵不平：
- 删掉 Boss 缩放 → 终局变简单 → 时间变短
- 决策界面计时 → 每局多出 10–30s（取决于你看菜单多久）
- 分支地图（§6）→ 单局节点数从固定 7 变成 7–8

建议实现后先跑 10 局取样再定线，不要现在拍。

> 💬 备注：

---

## 2. 受击经济学（连击税）

这是这个游戏的**核心不变式所在**：屏幕上任何一个 `+X.Xs` 价签，必须和实际扣的秒数逐字相同。

### 2.1 报价公式

```
本次惩罚 = 底价(base) × 惩罚倍率(penMult) × 连击税(comboTax)
连击税  = 1.3 ^ 连击层数(streak)
```

🆕 **玩家处于无敌时（受击无敌 / 冲刺中）：不扣秒，且价签完全隐藏——不显示任何文字。**
（原来会显示绿色的"免费"，现在直接什么都不画。）

🆕 **价签上的数字 = 此刻挨这一下的实际扣秒，已经乘过 penMult 和连击税。**
不是底价，不是估算值。玩家读到多少，账本上就扣多少。

🎯 为什么去掉"免费"绿字：无敌只有 0.17–0.40 秒，绿字一闪而过，反而在最需要读屏的瞬间制造了一层视觉噪音。**价签消失本身就是最清晰的"现在不要钱"信号**——场上安静下来了。

> 💬 备注：

### 2.2 参数

| 参数 | 现值 | 含义 |
|---|---|---|
| `INV_TIME` 受击无敌 | **0.40s** | 挨打后的无敌时长 |
| 冲刺无敌 | **0.17s** | 整个冲刺过程完全无敌 |
| `HITSTUN` 受击僵直 🆕 | **0.18s** | 见 §3.4 |
| `TAX_WINDOW` 税窗口 | **5.0s** | 距上次受击多久内继续累进；超时归零 |
| `TAX_STEP` 税率 | **×1.3** | 每多挨一次，价码乘 1.3 |
| ~~受击后反击窗口~~ | 🆕 **删除** | 只有持有「反击」强化时才存在，见 §5 |

### 2.3 连击税实例（以冲锋兵 2.0s 底价为例）

| 第几下 | 倍率 | 实扣 |
|---|---|---|
| 1 | ×1.00 | 2.0s |
| 2 | ×1.30 | 2.6s |
| 3 | ×1.69 | 3.4s |
| 4 | ×2.20 | 4.4s |
| 5 | ×2.86 | 5.7s |

🎯 设计意图：无敌帧本质是一笔"退款"——挨一下 = 花 pen 秒买 0.4 秒的穿墙权。0.4s 压得刚好够脱离接触，太长会让"无脑硬吃"变成最优解。连击税就是防止玩家把无敌帧当免费穿墙权用。**保留。**

> 💬 备注：

---

## 3. 玩家角色

### 3.1 基础数值 `BASE`

| 参数 | 现值 | 说明 |
|---|---|---|
| `dmg` 伤害 | 12 | 每次挥砍 |
| `atkCd` 攻击间隔 | 0.36s | |
| `range` 攻击范围 | 78px | |
| `arc` 挥砍弧度 | 1.9 rad ≈ 109° | 扇形张角 |
| `spd` 移速 | 268 px/s | |
| `dashCd` 冲刺冷却 | 1.05s | |
| `penMult` 惩罚倍率 | ×1 | |
| `costMult` 消费倍率 | ×1 | |
| `exec` 处决阈值 | 0（关闭） | |
| `refund` 击杀返还 | 0s | |
| `dashSlow` 时停时长 | 0（关闭） | |
| `counterDmg` 反击加伤 | 0（关闭） | |
| `projectile` 掷刃 🆕 | 0（关闭） | |
| 碰撞半径 `r` | 13px | |

### 3.2 冲刺

| 参数 | 现值 |
|---|---|
| 持续 | 0.17s |
| 速度 | 940 px/s（总位移约 160px） |
| 无敌 | **全程** |
| 方向 | **只看光标，与移动键无关**，按下那一帧锁死，途中不能转向 |
| 触发键 | `空格` / 鼠标右键 |
| 视觉 | 角色周围蓝环 = 冷却进度 |
| 🆕 可取消僵直 | 见 §3.4 |

🎯 设计意图：冲刺方向锁死+只看光标，是为了让"往哪躲"变成一个必须提前算好的决定，而不是可以中途反悔的操作。想往身后躲就把光标甩到身后。

### 3.3 挥砍

| 参数 | 现值 |
|---|---|
| 触发 | 鼠标左键（按住可连续） |
| 判定 | 距离 < `range + 敌人半径`，且角度差 < `arc / 2` |
| 前冲 | 每次挥砍角色沿瞄准方向瞬移 **14px** |
| 命中效果 | 敌人击退 180 px/s（摩擦系数 0.002^dt），hitstop 0.03s |
| 🆕 打飞行弹 | **不能。子弹无法被任何手段清除。** |

🆕 **子弹只能靠走位和冲刺躲开。** 这条改动把弹幕从"可以处理的资源"变成了"必须读懂的空间约束"，直接抬高了射手和 Boss 环形弹幕的分量——见 §4.4 的可躲性验算。

### 3.4 受击反馈 🆕

| 效果 | 数值 |
|---|---|
| **受击僵直 `HITSTUN`** 🆕 | **0.18s** 🎚️ |
| 击退自身 | 260 px/s（沿远离伤害源方向） |
| 屏幕震动 | 16 |
| 顿帧 hitstop | 0.07s（期间 dt × 0.12） |
| 红闪 | 0.35s |
| 移动摩擦 | v × 0.0016^dt |
| ~~黄色反击高亮~~ | 🆕 删除（只有持「反击」强化时才亮） |

🆕 **僵直规则**

1. 僵直期间**移动输入无效、攻击无效**
2. 击退位移**照常生效**——你会被打飞，只是不能自己走
3. **可被冲刺取消**：受击后 **0.09s** 起（僵直过半）可以按冲刺打断僵直，前提是冲刺冷却已就绪
4. 僵直（0.18s）**必须短于**无敌（0.40s），所以僵直结束后你还有 **0.22s 无敌**可以用来脱离

🎯 设计意图：原版挨打几乎没有操作代价——被打飞的同时还能立刻反向走回来，"硬吃一下换个位置"甚至是划算的。僵直给受击装上了一个真实的操作成本，让连击税的威慑第一次落到手上。而"冲刺可取消"保证它是**惩罚**而不是**死刑**：你有一个技术性的出口，但要付冲刺冷却的代价。

✅ **已定：围殴护栏。连击层数 ≥ 3 时，僵直减半到 0.09s。**
理由：冲刺在冷却中 + 被多个敌人围住时，0.18s 僵直 + 0.4s 无敌走完你可能还在原地，连击税会无限叠。护栏保证越被围，越容易挣脱。

> 💬 备注：

---

## 4. 敌人

🆕 **所有敌人数值为固定值，不随时间缩放。** 唯一的倍率是精英房的 ×1.35 血量。

### 4.1 冲锋兵 `charger`（三角形）

| 参数 | 现值 |
|---|---|
| 血量 | 32 |
| 半径 | 15 |
| **时间惩罚** | **+2.0s** |
| 移速 | 96 px/s |

**行为循环**

1. `idle` — 直线追击玩家（距离 > 40 才动）。初始冷却 `rnd(0.3, 1.4)`
2. 触发条件：距离 < 400 且冷却好了 且 **视线未被墙挡住** → 进入预警
3. `tel` 预警 **0.62s** — 画红色锁定射线（长 330px），**方向在预警开始那一帧就锁死**
4. `dash` 冲刺 **0.40s** @ **760 px/s**（约 300px）— 接触判定持续生效
5. `rec` 硬直 **0.55s** → 回 idle，冷却 `rnd(0.5, 1.1)`

🎯 破解方式：方向在预警时就锁死，侧移一步即可。

### 4.2 射手 `shooter`（方块）

| 参数 | 现值 |
|---|---|
| 血量 | 24 |
| 半径 | 13 |
| **时间惩罚（子弹）** | **+1.5s** |
| 移速 | 118 px/s |

**行为循环**

1. `idle` — 走位：距离 < 200 后退 / 距离 > 330 靠近 / 中间距离绕圈（垂直于玩家方向）
2. 触发：冷却好 且 距离 < 460 且 视线不被挡 → 预警
3. `tel` 预警 **0.48s** — 红色**虚线**锁定射线（长 520px），方向锁死
4. 发射：1 发红弹 → 回 idle，冷却 `rnd(1.8, 2.6)`

**子弹**：半径 6，速度 350 px/s，存活 3s，撞墙消失，🆕 **不可被打掉**

🎚️ 待调：子弹不能再被清除后，射手的实际威胁上升了一档。冷却 `rnd(1.8, 2.6)` 可能要往上调，或者把子弹存活时间从 3s 砍到 2.2s（缩短射程到约 770px，让远处的射手不再能隔着半个场子骚扰）。

### 4.3 重甲 `brute`（六边形）

| 参数 | 现值 |
|---|---|
| 血量 | 78 |
| 半径 | 24 |
| **时间惩罚** | **+4.0s** |
| 移速 | 64 px/s |

**行为循环**

1. `idle` — 追击（距离 > 78 才动）
2. 触发：距离 < 130 且冷却好 → 预警（**不检查视线**）
3. `tel` 预警 **0.95s** — 脚下红圈从 0 向外扩到半径 132
4. 爆发：**扩满瞬间**，半径 132 内**全圈**伤害
5. `rec` 硬直 **0.8s** → 冷却 `rnd(0.8, 1.6)`

### 4.4 BOSS（八边形）

| 参数 | 现值 |
|---|---|
| 🆕 血量 | **840**（固定）🎚️ |
| 半径 | 42 |
| 🆕 移速 | **88 px/s**（固定）🎚️ |
| **接触伤害** | **+5.0s** |

🎚️ 关于这两个数：原来是 `420 × bossScale` 和 `78 × (1+(sc−1)×0.25)`。一个 S 级速通（<150s）对应 `bossScale ≈ 2.0`，所以取 840 / 88 让**高手的体验保持不变**，而慢速玩家不再被额外惩罚。需要试玩确认 840 血打起来不会太拖。

**招式选择**（每次 idle 结束时随机，初始冷却 1.2s）

| 概率 | 招式 |
|---|---|
| 38% | 环形弹幕 `telBurst` |
| 34% | 三连冲锋 `telCharge` |
| 28% | 三段震波 `telSlam` |

idle 时：距离 > 200 才追击。

#### 招式 A · 环形弹幕（**+2.5s**）

- 预警 **0.8s** — 身周画一个向内收缩的圆
- 🆕 发射 **12 发**均匀环形弹，起始角度随机
- 弹速 280 px/s，半径 7，存活 4s
- 后摇冷却 1.0s，震屏 12

✅ **已定：12 发**（原 16 发）。

**可躲性验算**（子弹不可打掉之后，这条从"手感问题"升级成"能不能通关"）

环形弹在距 Boss 半径 `r` 处，相邻两弹的间隙 ≈ `2πr/n − 2×7`。
玩家直径 26px，要从缝里钻过去需要间隙 > 26px：

| 发数 | 可钻缝的最小半径 |
|---|---|
| 16 发 | r > 103px |
| **12 发（采用）** | **r > 77px** |

Boss 半径 42，所以 12 发时**贴脸站位（离体表 35px 外）就已经有缝可钻**，不必强行押着冲刺冷却。冲刺无敌仍然是最稳的解，但不再是唯一解。

#### 招式 B · 三连冲锋（**+5.0s** 接触）

- 预警 **0.7s** — 粗红线指示方向（长 700px），方向锁死
- 冲锋 **0.5s** @ **700 px/s**（约 350px）
- **连续 3 次**，第 2、3 次预警缩短到 **0.38s** 且**重新锁定玩家当前位置**
- 结束冷却 1.3s

#### 招式 C · 三段震波（**+3.0s**）

- 预警 **1.0s** — 三个同心圆同时向外扩
- 落地：三道环依次生成，半径 **90 / 175 / 260**，间隔 **190ms**
- 判定带：距 Boss 中心 `[半径−66, 半径+26]` 范围内命中（宽约 92px）
- 后摇冷却 1.1s，震屏 16

🆕 实现要求：三段的间隔必须走**游戏时钟**，不能用 `setTimeout`。原版用真实时间计时，开「时停」或触发顿帧时视觉预警和实际判定会错位。

**Boss 阶段化：暂不做。** 先把单形态跑通，后面再加怪、加阶段。

> 💬 备注：

---

## 5. 强化（共 13 个）

**同一局内不重复**（已持有的不再出现）。价格是商店价，战斗房掉落免费。

| 名称 | 商店价 | 效果 | 收益/代价 |
|---|---|---|---|
| **利刃** | 14s | 伤害 ×1.6，受击惩罚 ×1.4 | 打得快，挨打更贵 |
| **疾风** | 16s | 移速 ×1.16，冲刺冷却 ×0.65 | 纯机动 |
| **韧体** | 15s | 受击惩罚 ×0.65，伤害 ×0.85 | 挨打便宜，打得慢 |
| **长刃** | 12s | 攻击范围 ×1.45，弧度 ×1.15 | 纯攻击距离 |
| **连斩** | 14s | 攻击间隔 ×0.6，伤害 ×0.9 | DPS ×1.5 |
| **处决** | 20s | 敌人血量 < 30% 时被斩击**立即斩杀**（Boss 无效） | 省收尾时间 |
| **贪婪** | 13s | 此后所有秒数消费 ×0.65 | 越早拿越值 |
| **时停** | 18s | 冲刺时全场减速 60%，持续 0.55s | 救命 + 输出窗口 |
| **反击** | 11s | 受击后 2 秒内伤害 +85% | 有意的博弈：主动挨打换输出窗口 |
| **精算** | 15s | 每击杀一个敌人返还 **0.5s**（Boss 无效） | 直接减总时间 |
| 🆕 **掷刃** | **17s** 🎚️ | 每次挥砍额外向瞄准方向发射一枚刃弹 | **改变站位逻辑** |
| 🆕 **掠影** | **16s** 🎚️ | 冲刺穿过敌人时造成 `dmg × 1.2` 伤害 | **改变冲刺的用途** |
| 🆕 **蓄力** | **15s** 🎚️ | 按住左键蓄力，松开打出 360° 全向斩 | **改变攻击的节奏** |

🆕 **反击**：受击后的 2 秒窗口**只有持有这个强化时才存在**。没有它的时候，受击不再产生任何"反击状态"（角色也不会变黄）。

### 🆕 5.1 三个改变操作方式的强化

其余 10 个强化都只是在调数字，**没有一个改变你怎么玩**。这三个各自动了一条不同的操作前提。

#### 掷刃 — 动"输出必须贴身"这条前提

| 参数 | 值 |
|---|---|
| 商店价 | 17s 🎚️ |
| 触发 | 挥砍时**自动附带**，不占新按键 |
| 刃弹伤害 | `dmg × 0.55` |
| 刃弹速度 | 620 px/s，半径 5 |
| 存活 | 1.1s（有效射程约 680px） |
| 消失条件 | 撞墙 / 命中一个敌人 |

🎯 拿了它之后，风筝射手、隔墙消耗、边打边撤都成立了，走位目标从"贴上去"变成"控距"。
和**连斩**（攻速 ×0.6）叠加会变成一台弹幕机器——有意留的组合。和**长刃**是竞争关系（都在解决距离问题），也是有意的。

#### 掠影 — 动"冲刺是纯防御"这条前提

| 参数 | 值 |
|---|---|
| 商店价 | 16s 🎚️ |
| 触发 | 冲刺过程中身体与敌人重叠 |
| 伤害 | `dmg × 1.2` |
| 限制 | **同一次冲刺对同一个敌人只结算一次** |

🎯 冲刺原本是纯逃跑键。拿了掠影之后，"往敌人堆里冲"第一次变成了正收益操作——你会开始主动穿插，而不是只往空地躲。它和冲刺无敌天然协同：穿过去的那一瞬间你是无敌的。

#### 蓄力 — 动"按住左键连点"这条前提

| 参数 | 值 |
|---|---|
| 商店价 | 15s 🎚️ |
| 蓄满时间 | **0.5s** |
| 全向斩伤害 | `dmg × 2.6` |
| 全向斩范围 | `range × 1.15`，**360° 无死角** |
| 后摇 | `atkCd × 1.6` |

**按键语义改变**（这是它的代价）：

| 操作 | 持有蓄力前 | 持有蓄力后 |
|---|---|---|
| 按住左键 | 按 `atkCd` 节奏**连续挥砍** | **停止自动挥砍**，开始蓄力 |
| 短按松开（< 0.5s） | — | 普通挥砍 |
| 按住 ≥ 0.5s 后松开 | — | 360° 全向斩 |

🎯 这是三个里唯一有**明确代价**的：你失去了"按住不放自动输出"的省心打法，换来一个能同时清掉四面围殴的大招。被围时它是救命稻草，但蓄力的 0.5s 里你完全没有输出——什么时候敢蓄，就是这个强化的全部游戏性。

### 5.2 强化池深度

⚠️ 13 个强化，一局能拿到 6–9 个，**第二局基本见全**。这条还没解决。等玩法跑通后要么扩池到 20+，要么给强化加"同一个强化可叠加/进化"的机制。先记着。

> 💬 备注：

---

## 6. 🆕 关卡结构 — 分支地图 + 可复现 Seed

**整节重写。** 原来的"7 个线性节点 + 每次通过后抽 2–3 张路线卡"改为杀戮尖塔式的**一次性生成的分支地图**。

### 6.1 为什么要改

线性节点 + 随机抽卡有两个问题：
1. **看不见未来**。你不知道下一个是不是商店，所以"现在要不要买"永远是在信息缺失下瞎猜，而不是规划。
2. **不可复现**。竞速游戏必须能比较——同一张地图、同一批强化，才能比出谁快。抽卡式随机做不到。

分支地图同时解决两个：整张图开局就摊开给你看，路线是**规划**出来的；一个 seed 就能完整还原一局。

### 6.2 地图结构

| 参数 | 值 |
|---|---|
| 层数 | **8 层** 🎚️ |
| 列数 | 7 |
| 每层节点数 | 1–4（由路径生成决定） |
| 路径条数 | 6 |
| 第 1 层 | 固定全部为**战斗房** |
| 第 8 层 | 固定为**唯一的 Boss 节点**（所有路径汇聚） |
| 第 7 层 | 固定为**休整层**：只出商店或时间修复站 |

**生成算法**（全部走 seeded PRNG）

1. 用 seed 初始化 PRNG
2. 生成 6 条路径：每条从第 1 层随机一列出发，逐层向上，每步可走到 `列−1 / 列 / 列+1`
3. 路径经过的格子成为节点；不同路径重合则共用同一个节点
4. **边不允许交叉**（杀戮尖塔的经典约束，否则地图读不懂）
5. 按 §6.3 的权重给每个节点分配房型

### 6.3 房型权重与约束

第 2–6 层按权重随机分配：

| 房型 | 权重 |
|---|---|
| 战斗房 | 40% |
| 精英房 | 20% |
| 时间修复站 | 15% |
| 秒 · 商店 | 13% |
| 捷径门 | 12% |

**硬约束**（生成时强制满足，不满足就重roll该节点）

- 第 1 层：全部战斗房
- 第 2 层：不出精英房
- 第 7 层：只出商店或修复站
- 沿同一条路径，**商店和修复站不能连续出现两次**
- 每张地图至少有 1 个商店、至少 2 个精英房

### 6.4 房型定义

| 房型 | 代价 | 收益 |
|---|---|---|
| **战斗房** | 战斗消耗的游戏时间 | 清空后 **2 选 1** 免费强化 |
| **精英房** | 更长的战斗时间 | 清空后 **3 选 1** 免费强化 |
| **时间修复站** | **12s** × costMult | 消除已累计 `tPen` 的 **40%** |
| **秒 · 商店** | 各强化明码标价 | 一次开 3 个，可全买可空手走 |
| 🆕 **捷径门** | **9s** × costMult | **一条跨层的边**：从第 N 层直接跳到第 N+2 层，少打一场也少拿一个强化 |

🆕 **捷径门的语义变了**。原来是"跳过下一个节点"这种抽象说法；在地图上它现在是一条**画出来的斜长边**——你能亲眼看见它跳过了哪一层、绕开了哪个精英房、也错过了哪个商店。决策从"要不要省时间"变成"要不要放弃那条路上的东西"，具体多了。

### 6.5 敌人配置

按**层数**递增（不再随累计时间缩放）：

| 房间 | 敌人组成 | 血量倍率 |
|---|---|---|
| **战斗房** | `2 + 层数` 个（⅔ 冲锋兵 / ⅓ 射手）；层数 ≥ 3 时额外 +1 重甲 | ×1 |
| **精英房** | `2 + floor(层数/2)` 个重甲 + `2 + 层数` 个（冲锋兵/射手各半） | **×1.35** |

第 6 层战斗房 = 8 个杂兵 + 1 重甲。精英房 = 5 重甲 + 8 杂兵。

⚠️ 仍然只有"数量堆叠"这一种难度曲线，敌人种类从头到尾就 3 种。等核心跑通后再加怪。

### 6.6 商店细节

- 一次开出 3 个强化，买走后该位置留空（不补货）
- 价格 = `max(3, round(基础价 × costMult))`
- **永远买得起**——因为代价直接记在成绩上，不存在"钱不够"
- 🆕 **逛店期间时钟在走**（§1.1）

🎯 设计意图：取消"买不起"这个概念，让每次购买都是纯粹的价值判断，而不是资源管理。加上逛店计时之后，这个判断还必须**快**——你不能站在货架前把账算到底。

### 6.7 🆕 Seed

| 项 | 规格 |
|---|---|
| 格式 | 32 位无符号整数，也接受任意字符串（hash 成整数） |
| 输入 | 标题页可手动填；留空则随机 |
| 显示 | HUD 角落常驻 + 结算页可一键复制 |
| PRNG | `mulberry32`（32 位、快、够均匀，**不用 `Math.random()`**） |

**Seed 决定的内容**

- 地图拓扑与房型分配
- 每个房间的墙体布局
- 敌人生成位置
- 强化池的抽取顺序（战斗掉落 + 商店货架）
- Boss 招式序列

**两条独立随机流**（这条很关键）

| 流 | 何时派生 | 作用 |
|---|---|---|
| `mapRng` | 开局一次，从 seed 直接派生 | 整张地图 + 全部房型 + 强化预抽序列，**开局就全部定完** |
| `combatRng` | 进入每个房间时，从 `hash(seed, nodeId)` 派生 | 该房间的墙体、敌人生成点、Boss 招式 |

🎯 为什么要分流：如果只有一条流，玩家在战斗里多打一发子弹、多触发一次随机，后面的地图和商店内容就全变了——同一个 seed 两个人跑出来不一样，竞速就没法比。按 `nodeId` 派生保证了**每个房间的随机与玩家在别处做了什么完全无关**。

**这条是竞速模式的地基**：`seed + 总时间` 才构成一个可验证、可比较的成绩。

> 💬 备注：

---

## 7. 场地

- **尺寸**：1000 × 620（固定，等比缩放到窗口）
- **墙体布局**：进房间时由 `combatRng` 从 5 种预设里抽 1 种

| # | 布局 |
|---|---|
| 1 | 左上、右上两个方块 + 中下一个横块 |
| 2 | 左右两根竖长条 |
| 3 | 上下两根横条 + 左右两个方块 |
| 4 | 四角各一个方块 |
| 5 | **空场**（无掩体） |

- **玩家出生点**：(500, 530)，屏幕正下方
- **敌人生成**：x ∈ [80, 920]，y ∈ [60, 400]，且距玩家 > 240px（最多重试 40 次）
- 墙体会阻挡冲锋兵/射手的视线（不阻挡重甲和 Boss）
- 子弹撞墙消失

✅ **已定：加布局白名单。**

| 房间 | 允许的布局 | 理由 |
|---|---|---|
| 战斗房（层数 ≤ 2） | 全部 5 种 | 早期敌人少，空场无所谓 |
| 战斗房（层数 ≥ 3） | 1 / 2 / 3 / 4（**排除空场**） | 射手变多，子弹又打不掉，必须有掩体断视线 |
| **精英房** | 1 / 3 / 4（**排除空场和第 2 种**） | 多个重甲的 132px 爆炸圈需要可绕的掩体；第 2 种（两根竖长条）中间空档太大 |
| **Boss 房** | **5（空场）固定** | 三连冲锋和三段震波需要完整的场地，掩体会让判定读不清 |

> 💬 备注：

---

## 8. 报价 UI（核心表达）

**场上每一个会收你钱的东西都挂着实时价签**，上面的数字就是你此刻挨这一下**实际**会被扣的秒数，已经乘过强化倍率和连击税。

| 状态 | 显示 |
|---|---|
| 常态 | 红色 `+2.0s` |
| 连击税生效中 | **橙色加粗** `+2.6s` |
| 🆕 玩家无敌中 | **完全不显示**（原来是绿色"免费"） |

- **敌人**：脚下 10px 字号价签，恒定显示
- **子弹**：9px 字号，透明度 = `clamp(1 − (距玩家 − 100) / 200, 0, 1)`，越靠近越清楚（防止 Boss 弹幕糊成一片）
- **Boss 分招式报价**：起手三层环 → `+3.0s`；起手收缩圆 → `+2.5s`；冲锋接触 → `+5.0s`
- 🆕 HUD 右上角**删除**「BOSS 强度 ×N」；改为显示**当前层数 / 8** 与 **seed**

🎯 这是整个游戏最核心的表达。**必须保住的不变式**：屏幕显示的秒数 === 实际扣的秒数。现有的 [tests/](tests/) 就是专门守这个的，重构后要继续守，并新增一条：**无敌期间必须一个字都不画**。

> 💬 备注：

---

## 9. 手感 / 演出参数

| 参数 | 现值 |
|---|---|
| 🆕 受击僵直 | 0.18s（0.09s 后可被冲刺取消）🎚️ |
| 顿帧 hitstop（受击） | 0.07s，期间 dt × 0.12 |
| 顿帧 hitstop（命中敌人） | 0.03s |
| 屏幕震动（受击） | 16 |
| 屏幕震动（重甲爆发） | 10 |
| 屏幕震动（Boss 弹幕） | 12 |
| 屏幕震动（Boss 震波） | 16 |
| 震动衰减 | × 0.0005^dt |
| 「时停」减速 | 敌人与子弹 dt × 0.4，持续 0.55s |
| 单帧 dt 上限 | 1/25 s（40ms） |
| 房间清空 → 结算界面延迟 | 0.45s |

> 💬 备注：

---

## 10. 操作

| 键位 | 动作 |
|---|---|
| `W A S D` / 方向键 | 移动 |
| 鼠标移动 | 瞄准 |
| 鼠标左键 | 挥砍（🆕 持「掷刃」时同时发射刃弹） |
| `空格` / 鼠标右键 | 朝光标冲刺（全程无敌，🆕 可取消受击僵直） |
| `1` `2` `3` `4` | 选卡 |
| 🆕 鼠标 | 在地图上点选下一个节点 |

### 🆕 暂停键 — 遮蔽式

✅ **已定：`ESC` 遮蔽式暂停。**

| 规则 | |
|---|---|
| 时钟 | **停止**（tPlay 不累加） |
| 画面 | **完全不透明的遮罩盖住整个战场**，只显示"已暂停"和菜单项 |
| 可用操作 | 继续 / 重开本局 / 回标题 |

🎯 为什么必须遮蔽：决策界面开始计时之后（§1.1），一个普通的半透明暂停界面就是绕过它的后门——按 ESC 停表，慢慢看清场上所有敌人的状态和价签，想好了再继续。遮蔽式保留了"我需要离开一下"这个正当需求，同时让暂停**没有任何信息收益**。

**其它缺失**：没有手柄支持、没有触屏支持。暂不做。

> 💬 备注：

---

## 11. 短板清单（更新状态）

| # | 短板 | 状态 |
|---|---|---|
| 1 | 内容量太薄（3 杂兵 + 1 Boss + 11 强化） | ⏸️ 推迟——先把核心跑通再加怪 |
| 2 | 难度曲线只有"数量堆叠"一根旋钮 | ⏸️ 推迟 |
| 3 | 强化全是被动数值 | 🔄 部分解决——加了「掷刃」，还需要更多 |
| 4 | 决策界面不计时 | ✅ **已解决**（§1.1） |
| 5 | 经济决策不够纠结 | ✅ **已解决**——决策界面计时让你没时间把账算到底（§6.6） |
| 6 | 死亡螺旋无缓冲 | ✅ **已解决**——删掉了全部时间缩放（§1.2） |
| 7 | Boss 无阶段 | ⏸️ 推迟 |
| 8 | 没有元进程 | 🔄 seed 系统（§6.7）打好了竞速排行的地基，但排行本身还没做 |
| 9 | 🆕 受击没有操作代价 | ✅ **已解决**——加了僵直（§3.4） |
| 10 | 🆕 地图不可复现，无法比较成绩 | ✅ **已解决**——seed 系统（§6.7） |

> 💬 备注：

---

## 12. 你想加的新东西

> 💬 在这里随便写，不用管格式。想到什么写什么，我会整理成可实现的规格。





---

## 13. 附：重构后必须保留的技术不变式

无论玩法怎么改，下面这些是我在重构时会强制保证的（跟玩法设计无关，不用你操心）：

1. **报价 === 实扣**：屏幕上任何 `+X.Xs` 与账本扣款走同一条公式、同一个格式化函数，并由测试守住。🆕 无敌期间必须一个字都不画。
2. **所有游戏计时走游戏时钟**，不用 `setTimeout`。暂停、顿帧、时停都能正确影响所有计时器。
3. **逻辑与渲染分离**：渲染函数不修改游戏状态。
4. **数值全部数据化**：所有玩法参数集中在配置文件里，调平衡不用碰逻辑代码。
5. **固定步长物理**：帧率不影响手感与判定结果，且回放可复现。
6. 🆕 **全局禁用 `Math.random()`**：所有随机必须从 seed 派生（§6.7）。这条会用 lint 规则强制，因为一个漏网的 `Math.random()` 就会让整个竞速模式失效，而且极难排查。

---

## 14. 裁决记录（已全部定案）

| # | 章节 | 问题 | 裁决 |
|---|---|---|---|
| 1 | §3.4 | 连击层数 ≥3 时僵直减半的护栏 | ✅ **加** |
| 2 | §4.4 | Boss 环形弹幕发数 | ✅ **减到 12 发**（缝隙门槛 r > 77px） |
| 3 | §5.1 | 操作型强化选哪个 | ✅ **掷刃 + 掠影 + 蓄力 全加**（池 → 13 个） |
| 4 | §7 | 墙体布局白名单 | ✅ **加**（Boss 房固定空场） |
| 5 | §10 | 暂停键 | ✅ **遮蔽式** |
| 6 | §1.3 | 评级线失准 | ✅ 先沿用旧线，实现后取样再校准 |
| 7 | §6.2 | 地图规模 8 层 / 7 列 / 6 路径 | ✅ 先按这个做，试玩后调 |

### 实现后仍待试玩校准的数值（🎚️）

Boss 血量 840 / 移速 88 · 受击僵直 0.18s · 掷刃 17s / 掠影 16s / 蓄力 15s 的定价 · 射手冷却与子弹存活 · 评级分界线 · 地图规模
